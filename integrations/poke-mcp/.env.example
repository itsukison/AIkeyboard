# Protect the public HTTPS tunnel with a secret you generate yourself.
POKE_AUTH_MODE=api-key
POKE_MCP_API_KEY=replace-with-a-long-random-secret
BUFFER_API_KEY=replace-with-your-buffer-api-key
# Optional: lock the bridge to the UUID Poke sends in X-Poke-User-Id.
POKE_ALLOWED_USER_ID=
PORT=3000
