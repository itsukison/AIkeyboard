# Archive

Dormant or restorable code lives here when it is intentionally excluded from
all build targets.

Use this only for material with a clear restore path. Historical docs belong
under `docs/archive/`; reference screenshots belong under `docs/assets/`.
